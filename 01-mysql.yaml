- name: Installing MySQL database for expense project
  hosts: mysql
  become: yes
  tasks: 
  - name: install mysql
    ansible.builtin.dnf:
      name: mysql-server
      state: installed

  - name: Start and enabling the mysql service
    ansible.builtin.service:
      name: mysqld
      state: start
      enabled: true